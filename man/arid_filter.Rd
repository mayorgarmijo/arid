% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arid_filter.R
\name{arid_filter}
\alias{arid_filter}
\title{Filter ARID data by geographic and chronological variables}
\usage{
arid_filter(
  tables = c("humans", "animals", "plants"),
  ecozone = NULL,
  admin_region = NULL,
  period = NULL,
  period_broad = NULL,
  locality = NULL
)
}
\arguments{
\item{tables}{Character vector. One or more of \code{"humans"}, \code{"animals"},
\code{"plants"}. Defaults to all three.}

\item{ecozone}{Character vector. One or more ecozones to keep:
\code{"Coast"}, \code{"Lowlands"}, \code{"Precordillera"}, \code{"Altiplano"}.}

\item{admin_region}{Character vector. One or more administrative regions:
\code{"Arica y Parinacota"}, \code{"Tarapaca"}, \code{"Antofagasta"}.}

\item{period}{Character vector. One or more sub-periods
(e.g. \code{"Early Archaic"}, \code{"Late Formative"}).}

\item{period_broad}{Character vector. One or more broad period categories
(e.g. \code{"Archaic"}, \code{"Formative"}, \code{"Late Intermediate"}, \code{"Late"}).}

\item{locality}{Character vector. One or more localities
(e.g. \code{"Lower Azapa Valley"}).}
}
\value{
A filtered data frame.
}
\description{
Calls \code{arid_merge()} internally and filters the result by one or more
contextual variables. At least one filter argument must be provided.
Multiple values per argument are accepted (OR logic within each argument,
AND logic across arguments).

Llama a \code{arid_merge()} internamente y filtra el resultado por una o más
variables contextuales. Debe proporcionarse al menos un argumento de filtro.
Cada argumento acepta vectores de valores (lógica OR dentro del argumento,
lógica AND entre argumentos).
}
\examples{
# Humans from the Altiplano
arid_filter(tables = "humans", ecozone = "Altiplano")

# Multiple ecozones and an administrative region
arid_filter(ecozone = c("Coast", "Lowlands"), admin_region = "Antofagasta")

# Broad period filter
arid_filter(period_broad = "Formative")

# Specific sub-period
arid_filter(period = "Early Archaic")

}
\seealso{
\code{\link[=arid_merge]{arid_merge()}} for the underlying combine function;
\code{\link[=arid_chronology]{arid_chronology()}} to assign dates to the result.
}
