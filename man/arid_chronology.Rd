% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arid_chronology.R
\name{arid_chronology}
\alias{arid_chronology}
\title{Assign chronological date ranges to ARID samples}
\usage{
arid_chronology(data, unit = c("BCE", "BP"))
}
\arguments{
\item{data}{A data frame from \code{arid_merge()} or \code{arid_filter()}.
Must contain a \code{lab_id} column.}

\item{unit}{Character. Output date system: \code{"BCE"} (default; negative =
BCE, positive = CE) or \code{"BP"} (cal BP).}
}
\value{
The input data frame with three new columns:
\describe{
\item{date_from}{Start of date range in the selected unit.}
\item{date_to}{End of date range in the selected unit.}
\item{date_source}{\code{"C14"} if a direct radiocarbon date was found in
\code{arid_c14}; \code{"period"} if the period range was used as a fallback.}
}
}
\description{
Adds \code{date_from}, \code{date_to}, and \code{date_source} columns to a data frame
returned by \code{arid_merge()} or \code{arid_filter()}. For samples with a direct
radiocarbon date (\code{has_c14 == TRUE}), the calibrated range from \code{arid_c14}
is used. For the rest, the cultural period range (\code{period_from} /
\code{period_to}) is used as a fallback. Results are returned in the selected
unit system.

Agrega columnas \code{date_from}, \code{date_to} y \code{date_source} a un data frame
generado por \code{arid_merge()} o \code{arid_filter()}. Para muestras con fechado
radiocarbónico propio (\code{has_c14 == TRUE}), se usa el rango calibrado de
\code{arid_c14}. Para el resto, se usa como alternativa el rango del período
cultural (\code{period_from} / \code{period_to}).
}
\details{
\strong{Date systems / Sistemas de fechas:}
\itemize{
\item All dates in ARID — including \code{period_from}/\code{period_to} and
the calibrated radiocarbon ranges in \code{arid_c14} — are stored in
\strong{BCE/CE} (negative = BCE, positive = CE).
\item When \code{unit = "BP"}, dates are converted to cal BP: \code{BP = 1950 - CE}.
}
}
\examples{
# Assign chronology in BCE/CE (default)
df <- arid_merge("humans")
df <- arid_chronology(df)
head(df[, c("sample_id", "date_from", "date_to", "date_source")])

# In cal BP
df_bp <- arid_chronology(arid_merge("humans"), unit = "BP")

}
\seealso{
\code{\link[=arid_merge]{arid_merge()}}, \code{\link[=arid_filter]{arid_filter()}}, \link{arid_c14}.
}
